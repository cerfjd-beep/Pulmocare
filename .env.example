# Server-only. Do not use NEXT_PUBLIC_ for these secrets.
# Enable Routes API and billing in the Google Cloud project before live use.
GOOGLE_MAPS_API_KEY=
# Random private operator token with at least 32 characters. Never commit the real value.
TRAVEL_QUOTE_ACCESS_TOKEN=
